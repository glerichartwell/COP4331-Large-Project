{
  "name": "backend",
  "version": "1.0.0",
  "description": "In this folder, we will store the JS files that render the actual pages and the server JS file.",
  "main": "db.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "heroku": "^7.59.2",
    "mongo": "^0.1.0",
    "nodemailer": "^6.7.1",
    "nodemon": "^2.0.15"
  }
}

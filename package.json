{
  "name": "backend",
  "version": "1.0.0",
  "description": "In this folder, we will store the JS files that render the actual pages and the server JS file.",
  "main": "db.js",
  "scripts": {
    "build-front": "cd frontend && npm run build",
    "install-frontend": "cd frontend && npm install --legacy-peer-deps",
    "heroku-postbuild": "npm run install-frontend && npm run build-front",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "firebase": "^9.6.0",
    "heroku": "^8.0.1",
    "mongo": "^0.1.0",
    "nodemailer": "^6.7.1",
    "nodemon": "^2.0.15"
  }
}
